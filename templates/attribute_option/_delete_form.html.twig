<form method="post" action="{{ path('app_attribute_option_delete', {'id': attribute_option.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ attribute_option.id) }}">
    <button class="btn">Delete</button>
</form>
