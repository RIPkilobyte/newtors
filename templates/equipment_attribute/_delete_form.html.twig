<form method="post" action="{{ path('equipment_attribute_delete', {'id': equipment_attribute.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ equipment_attribute.id) }}">
    <button class="btn">Delete</button>
</form>
